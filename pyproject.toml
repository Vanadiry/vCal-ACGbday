[project]
name = "vcal-acgbday"
version = "0.1.0"
description = "二次元角色生日日历数据生成与检查工具"
requires-python = ">=3.10"
dependencies = [
  "PyYAML>=6.0",
  "requests>=2.28",
  "jsonschema>=4.20",
]

[project.scripts]
vc-lint = "scripts.check.lint:main"
vc-uuid = "scripts.check.normalize_uuid:main"
vc-bday = "scripts.check.bday:main"
vc-dist-ics = "scripts.check.dist_ics:main"
vb-build = "scripts.build.build:main"

[project.optional-dependencies]
dev = [
  "ruff>=0.6",
  "icalendar>=7.0",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.ruff]
target-version = "py310"
line-length = 100
src = ["scripts"]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B", "SIM"]
ignore = [
  "E501",
]

[tool.ruff.lint.per-file-ignores]
"scripts/check/*.py" = [
  "B008",
]

[tool.setuptools]
packages = ["scripts", "scripts.lib", "scripts.check", "scripts.build"]
